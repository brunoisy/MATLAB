function [ F ] = multi_fd(param, X)
%MULTI_FD Summary of this function goes here
%   Detailed explanation goes here
x0 = param(1);
Lcs = param(2:end);
F = zeros(length(X),1);
for i = 1:length(Lcs)
    F() = fd(Lcs(i)-x0, X()
end

end

